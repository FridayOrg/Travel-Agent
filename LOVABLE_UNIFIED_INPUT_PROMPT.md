Paste the text below into your Lovable project's AI chat. This REPLACES/supersedes all previous
partial input-handling prompts — treat it as the single source of truth for this fix.

--------------------------------------------------------------------------------

BUG: the chat text input (and voice input) does not work whenever the assistant shows
button-based options — on EVERY such screen in the app, not just one. Typing or speaking an
answer does nothing; only clicking a button works. This must be fixed with ONE shared code path
used everywhere, not patched screen by screen.

FULL INVENTORY OF EVERY BUTTON-BASED SCREEN IN THIS APP (audit against this list — confirm you
found all of them, and flag if you find any not listed here):

Category A — STATIC screens (fixed question/options, not generated by the AI each time):
  1. Destination — "Where would you like to go?" (Dubai / London / Other)
  2. Traveller type — "Who are you travelling with?" (Family / Friends / Solo / Couple)
  3. Month — "When are you planning to travel?" (Aug-Sep / Oct-Nov / Dec-Jan / Other)
     (1-3 are shown together as one intake form)
  4. Hotel travellers count — "How many travellers?" (2 Adults / 2 Adults + 1 Kid / Other)
  5. Hotel budget — "What's your budget range per night?" (Budget / Mid-range / Luxury / Other)
  6. Hotel dates — check-in/check-out date picker

Category B — DYNAMIC screens (the AI agent generates these live, mid-conversation, as a JSON
message in this exact shape: {"type": "clarifying_question", "stage": "...", "question": "...",
"options": [...], "allow_other": true|false} — these can appear at any point in the chat, not
just fixed steps):
  7. Trip duration — "How many days is your trip?" (2-3 days / 4-6 days / 7+ days / Other)
  8. Itinerary confirmation — "Are you okay with this itinerary?" (Yes, continue / Modify itinerary)
  9. Hotel style preference (optional, may not always appear) — "What kind of hotel setting
     would you prefer?" (Beach resort / City center high-rise / Serviced apartment / Other)
  10. Hotel confirmation — "Are you happy with this hotel?" (Yes, continue / Choose another hotel)
  11. Any other clarifying_question the agent generates in the future — new ones WILL appear as
      the app evolves, and must automatically work without further frontend changes (see
      Requirement 3 below).

Category C — other interactive elements that should behave the same way:
  12. Hotel recommendation cards' "Select Hotel" button
  13. The booking form (Name/Email/Phone) that appears via a {"type": "booking_form"} message

--------------------------------------------------------------------------------

ROOT CAUSE TO FIND AND ELIMINATE

Somewhere in the codebase there is very likely a single shared mechanism causing this — e.g. a
global "awaiting button selection" flag, the text input being conditionally disabled while any
button block is rendered, or the text-submit/voice-transcription handlers simply never being
wired to call the same function a button's onClick calls. Find that ONE root cause. Do not accept
a fix that only works for category A or only for category B — if your fix requires touching each
screen individually, you have not found the root cause.

--------------------------------------------------------------------------------

REQUIRED FIX

1. ONE SHARED HANDLER FOR ALL THREE INPUT METHODS, ON EVERY SCREEN ABOVE (both categories A
   and B, no exceptions):
   - Clicking any button
   - Typing text and submitting (Enter key or send button)
   - Voice input (mic transcription completing)
   All three must call the exact same function with the resolved text value. There must be zero
   screens where any of these three is wired differently or missing.

2. HOW THE SHARED HANDLER ROUTES, DEPENDING ON CURRENT STAGE:
   - If the current stage is one of the Category A static stages (INTAKE, HOTEL_TRAVELLERS,
     HOTEL_BUDGET, HOTEL_DATES): send the text to POST /api/session/{id}/static-answer (already
     documented in earlier prompts — handles matching, questions, and change requests).
   - Otherwise (any Category B dynamic clarifying_question is showing, or no buttons are showing
     at all): send the text to POST /api/session/{id}/message exactly as normal free-text chat
     already works. The backend/LLM agent already understands free-text answers to its own
     questions correctly (verified: e.g. typing "2 to 3 days" in response to "How many days is
     your trip?" is already handled correctly server-side) — the ONLY thing missing here is the
     frontend actually sending it instead of blocking it.
   - A button click is just this same call with the button's exact label as the text value —
     buttons are a convenience shortcut for the same function, never a separate code path.

3. FUTURE-PROOFING — STRUCTURAL, NOT JUST A BUG FIX
   Refactor so ALL button-based questions (both static and the dynamic clarifying_question ones)
   render through one shared, config/data-driven component — not one-off hardcoded JSX per
   screen. For Category B specifically, since the options come from the backend's own JSON at
   runtime, the button-rendering component and the text/voice input path must both read from
   that same "options" array — never a separate hardcoded list. This way, any NEW
   clarifying_question the agent generates in the future (new stage, new options) automatically
   gets working buttons AND working text/voice input with zero additional frontend code.

4. CHECK FOR SILENT ERRORS
   Open the browser console and try typing an answer on a button screen (both a Category A and a
   Category B screen). If there's a JS error (undefined function, broken state reference, a
   fetch call that's never actually made), that's very likely the root cause — fix it at the
   source, not by adding a workaround on top.

5. VERIFY WITH A FULL AUDIT — provide this back as a checklist/table
   After fixing, go through every single screen in the inventory above (all 13 items) and
   confirm PASS/FAIL for: (a) button click, (b) typed text, (c) voice input. All three must pass
   on all 13 before this is considered done. Report the completed checklist.
